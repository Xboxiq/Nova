# معاينة NOVA UI من الجوال

## الطريقة الأسرع

1. فك ضغط `nova-ui-react.zip` على الكمبيوتر.
2. افتح Terminal داخل مجلد المشروع.
3. ثبّت الحزم مرة واحدة:

```bash
npm install
```

4. شغّل خادم الجوال:

```bash
npm run dev:mobile
```

5. سيظهر عنوانان مشابهين للتالي:

```text
Local:   http://localhost:5173/
Network: http://192.168.1.20:5173/
```

6. تأكد أن الهاتف والكمبيوتر على شبكة Wi‑Fi نفسها.
7. افتح عنوان `Network` من Safari أو Chrome على الهاتف.

## معاينة النسخة الإنتاجية

```bash
npm run build
npm run preview:mobile
```

افتح عنوان `Network` الذي يظهر، وغالبًا يستخدم المنفذ `4173`.

## إذا لم يفتح الرابط

- استخدم عنوان `Network` وليس `localhost`؛ لأن `localhost` على الهاتف يعني الهاتف نفسه.
- اسمح لـNode.js بالاتصال عبر الشبكة المحلية عندما يسأل جدار الحماية.
- عطّل VPN مؤقتًا على الهاتف والكمبيوتر.
- تأكد أن الشبكة لا تستخدم عزل الأجهزة، وهو شائع في شبكات الضيوف.
- جرّب كتابة العنوان كاملًا مع `http://` والمنفذ.

إذا لم يعرض Vite سطر `Network`، احصل على عنوان الكمبيوتر يدويًا:

- Windows: شغّل `ipconfig` وابحث عن `IPv4 Address`.
- macOS: شغّل `ipconfig getifaddr en0`.
- Linux: شغّل `hostname -I`.

ثم افتح من الهاتف `http://عنوان-IP:5173`، مثل `http://192.168.1.20:5173`.

## ملاحظات

- المعاينة محلية ولا تنشر المشروع على الإنترنت.
- أي تعديل تحفظه على الكمبيوتر يظهر مباشرة على الهاتف أثناء تشغيل `dev:mobile`.
- الواجهة تبدأ بالعربية وRTL، ويمكن تبديل الاتجاه والمظهر من الشريط العلوي.
