# NOVA UI Implementation Audit

تاريخ الفحص: 16 آب 2026  
النطاق: تطبيق React وTypeScript بعد دمج مكتبة مدار، نظام NOVA Design OS، 72 نمطًا تفاعليًا و30 قسمًا من مدار، سبع حزم ثيم وثلاثة مستويات زجاج، مختبرا القرار والبناء V7، العربية والإنجليزية، النهار والليل، وبنية الجوال.

## النتيجة الحالية

النسخة الإنتاجية تُبنى وتعمل بنجاح بعد الدمج. سطح مدار خالٍ من مخالفات Axe في ست تركيبات ثيم وقياس، ولا توجد ثغرات NPM. أُعيد الفحص فعليًا باستخدام Chromium Headless على سطح المكتب بعرض 1440px وعلى جوال بعرض 390px.

| الفحص | النتيجة | الدليل |
|---|---:|---|
| TypeScript | ناجح | `npm run typecheck -- --force` |
| Production build | ناجح | `npm run build` باستخدام Vite 8.1.5 |
| Production server | ناجح | الصفحة وملف JavaScript الرئيس يعيدان HTTP 200 |
| Dependency audit | ناجح | `npm audit --audit-level=high`: صفر ثغرات |
| Axe على سطح مدار | ناجح | صفر مخالفات في 6 تركيبات × قسمين |
| Axe على الصفحة كاملة | ناجح | المخالفة التي كانت «قائمة سلفًا» أُصلحت، تفصيلها أدناه |
| Browser console | ناجح | صفر console errors وصفر page errors |
| 390px reflow | ناجح | 390px بلا overflow أفقي في light وnight وmint، وRTL وLTR |
| 1440px reflow | ناجح | `scrollWidth` يساوي `clientWidth` |
| تباين حزم الثيم | ناجح | كل الأزواج المقيسة في الحزم الخمس الجديدة ≥ 4.5:1 |
| قائمة المظهر | ناجح | تفتح، تطبّق الحزمة، وتُغلق بالنقر خارجها وبمفتاح Escape |
| سجل قبول الدمج | ناجح | 14 من 14 بوابة محقّقة بدليل مسجَّل في `gates/01-madar-merge.md` |
| سجل قبول عائلة الأثر | ناجح | 13 من 13 بوابة محقّقة بدليل مسجَّل في `gates/02-consequence-family.md` |

## كيف يُعاد الفحص

`npm run qa:madar` يشغّل الفحص كاملًا: التباين في الحزم السبع، وAxe على سطح مدار، وoverflow عند 390px و1440px في RTL وLTR، وقائمة المظهر، وأخطاء وقت التشغيل. يعيد رمز خروج غير صفري عند أي إخفاق. وسجلات القبول في `gates/`، ويُتحقّق منها بـ`node .claude/skills/unlazy/scripts/gate-check.mjs gates/<الملف>.md`.

## ما تم فحصه بعد الدمج

- سبع حزم ثيم على نظام توكنز واحد: `light` و`dark` من `tokens.css`، و`mint` و`coral` و`sky` و`iris` و`night` من `tokens/theme-packs.css`، كلها بأسماء `--nova-*` نفسها.
- ربط أسماء مدار عبر `src/madar/bridge.css`: قياس فعلي أثبت أن `--bg` و`--accent` تتبعان `--nova-canvas` و`--nova-action` في الحزم السبع كلها، فلا يوجد نظام لون ثانٍ.
- ثلاثة مستويات زجاج عبر `data-glass`: قياس `--glass-blur` أعاد 10px و20px و36px بالترتيب.
- قائمة المظهر في الشريط العلوي: سبعة خيارات، اختيار الحزمة يغيّر `data-theme` على جذر المستند، والاختيار يُحفَظ في `localStorage`.
- تحميل أقسام مدار الـ30 كسولًا: كل قسم حزمة JavaScript منفصلة، فحجم التحميل الأول لم يتضخّم بدخول المكتبة.
- تبديل الأقسام فعليًا: `توكنز اللون` و`مختبر الفيزياء` و`صلاحيات الإدارة` تُحمَّل وتُعرَض عند النقر.
- لوحة الأوامر تجد أقسام مدار: البحث عن `kinetics` يعيد نتيجتَي مدار ضمن مجموعتها الخاصة.
- عزل CSS: قواعد مدار على مستوى العناصر و`::selection` و`line-height` محصورة في `.madar-surface`، وقشرة NOVA لم تتغيّر طباعتها.
- عدم إدخال أي مضيف خطوط خارجي: أدوار العرض في مدار تُحَل إلى خطوط NOVA ذاتية الاستضافة بدل Google Fonts.
- الحفاظ على كل ما كان يُفحص في V7: البحث والفلاتر ولوحة الأوامر وتبديل الاتجاه والمختبران.

## تباين حزم الثيم

القياس فعلي من المتصفح على القيم المحسوبة، بنسبة WCAG.

| الزوج | light | dark | mint | coral | sky | iris | night |
|---|---:|---:|---:|---:|---:|---:|---:|
| ink / canvas | 14.82 | 16.61 | 14.23 | 14.05 | 14.37 | 13.89 | 12.66 |
| ink-secondary / surface | 6.88 | 9.60 | 6.75 | 6.53 | 6.79 | 6.55 | 6.00 |
| ink-tertiary / surface-quiet | 4.28 | 5.25 | 4.79 | 4.84 | 4.79 | 4.82 | 4.69 |
| on-action / action | 5.27 | 7.62 | 7.61 | 5.09 | 5.14 | 4.89 | 10.48 |
| action-ink / surface | 8.04 | 9.82 | 6.39 | 6.40 | 6.27 | 7.35 | 8.24 |
| on-ink-block / ink-block | 13.06 | 16.61 | 13.77 | 13.40 | 13.76 | 13.46 | 14.07 |
| danger / surface | 5.15 | 7.06 | 5.76 | 5.74 | 5.76 | 5.74 | 4.89 |
| success / surface | 6.12 | 8.70 | 4.92 | 4.91 | 4.92 | 4.90 | 5.93 |

القيم الأصلية لحزم مدار كانت ترسب في أربعة أزواج، فعُدّلت قبل الدمج: خُفض `ink-tertiary` في الحزم الفاتحة الأربع من `l≈0.578` إلى `0.525`، وخُفض `action` في `coral` من `0.62` إلى `0.55` وفي `sky` من `0.58` إلى `0.52`، ورُفع `danger` في `night` من `0.72` إلى `0.76`.

## مخالفة قائمة قبل الدمج

`color-contrast` على نص بحجم 9px في منطقتين من قشرة NOVA نفسها:

- نهاري: `.motion-workbench.is-compact .motion-metrics span` بنسبة 4.28:1 (`#5D747D` على `#EAF0F2`)، عنصران.
- ليلي: `.advanced-notices .notification-stack time` بنسبة 4.35:1 (`#91A8AF` على `#21414F`)، خمسة عناصر.

هذه المخالفة لم تكن من الدمج، وقد **أُصلحت لاحقًا** في تمريرة مكافحة الـslop: `--nova-ink-tertiary` دُكِّن في الحزمة الفاتحة من `#5d747d` إلى `#566c74`، وفي الداكنة من `#91a8af` إلى `#9db3b9`. أُلغي معها الاستثناء الذي كان يعفي الحزمة الفاتحة في `tools/qa/madar-qa.mjs`، ووُسّعت أزواج القياس لتشمل `surface` و`surface-raised` لا `surface-quiet` وحدها. التفصيل في `design-system/ANTI-SLOP-PASS.md`.

## Screenshots الحالية

- `qa-merge-desktop.png`: سطح مدار على سطح المكتب بعرض 1440px، ثيم نهاري، قسم البطاقات المميّزة.
- `qa-merge-packs-iris.png`: الحزمة البنفسجية مطبّقة على القشرة كاملة مع قسم الأزرار.
- `qa-merge-mobile-night.png`: الحزمة النيلية على جوال عربي بعرض 390px.
- `qa-consequence-light.png`: قسم الأثر نهاريًا عند 1440px، والإتلاف في منتصفه مع نافذة التراجع.
- `qa-consequence-night.png`: قسم الأثر على جوال نيلي بعرض 390px.
- لقطات V7 السابقة `qa-v7-*.png` وتقريرها `qa-v7-report.json` تبقى مرجعًا لحالة ما قبل الدمج.

## Anti-Slop declaration

هذه الواجهة لا يمكن أن تكون لمنتج عشوائي لأنها فهرس عربي تفاعلي يجمع 72 نمط NOVA بمصادرها الظاهرة مع 30 قسمًا من مدار، فوق طبقة توكنز واحدة تتيح تبديل سبع حزم ثيم ومستوى المادة على المكوّنات نفسها لحظيًا. مهمة البحث عن مكوّن وفحصه في حزمة ثيم ثم فتح مصدره تظل قابلة للإنجاز دون الاعتماد على المؤثرات.

## ملاحظات المصدر

- بحث 21st.dev من سطر الأوامر يحتاج جلسة دخول خاصة؛ تم الاعتماد على روابط المالك المباشرة مع الحفاظ على المصدر وتكييف السلوك بصريًا وتقنيًا مع NOVA.
- تاريخ مستودع مدار مدموج عبر `git subtree`، وحزمة التسليم الأصلية محفوظة في `archive/madar/`.
- حُدّث `package-lock.json` عبر `npm audit fix` لمعالجة ثغرتين ظهرتا في `postcss` و`nanoid` وهما تبعيتان لأداة البناء Vite. لم تتغيّر أي تبعية معلنة في `package.json`.
