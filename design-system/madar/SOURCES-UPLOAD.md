# المصدر: Glass Upload Card

**الأصل:** [Nazia-99/File-Upload-Ui](https://github.com/Nazia-99/File-Upload-Ui) — ثلاثة ملفات، 734 سطرًا: `index.html` و`script.js` و`style.css`. ليس بداخله مستودعات أخرى ولا روابط إلى غيره؛ هو مكوّن واحد.

## ما أخذناه، وما رفضناه

القاعدة هنا هي نفسها التي حكمت تمريرة Uiverse: **تُؤخذ الفكرة، لا التنفيذ.** كل لون ومسافة وحركة يُعاد التعبير عنها بتوكنز NOVA، وإلا صار الدمج حقن نظام لوني ثانٍ في مشروع له نظام واحد مقرَّر.

| العنصر في الأصل | الحكم | السبب |
|---|---|---|
| **المجلّد كجسم**: لوحان SVG وبطاقة بينهما | **مأخوذ** — والهندسة نفسها | فكرة أصيلة لا نظير لها في نوفا: للرفع **وجهة لها هيئة**، لا حقل إسقاط مجرّد |
| **الرفع كدورة حياة**: نسبة، وحالة، ومحمّل يتحوّل إلى علامة صحّ | **مأخوذ — وموسَّع** | `FileDropzone` القائم يستقبل الملفات ثم تجلس بلا مصير. الأصل يعرض دورة الحياة لكنها **وهمية**: عدّاد إلى 100 لا يرفع شيئًا |
| خلفية البطاقة: **ستّة تدرّجات شعاعية** مكدّسة | **مرفوض** | هذا حرفيًا نمط المجموعة 06 الذي أزلناه من قشرة نوفا قبل ساعات. إعادته من الباب الخلفي تنقض التمريرة كلها |
| حدّ دوّار بتدرّج مخروطي حول البطاقة | **مرفوض — موجود** | `k99-border` في `bridge.css` يفعل هذا، والمكتبة لا تحتاج نسخة ثانية |
| `Poppins` من Google Fonts + Font Awesome من CDN | **مرفوض** | نوفا تستضيف خطوطها ذاتيًا ولا تُدخل مضيفًا خارجيًا. الأيقونات مرسومة SVG داخل المكوّن |
| ألوان `hsl` بنفسجية/قرمزية مكتوبة يدويًا في 40 موضعًا | **مرفوض** | المكوّن لا يعلن لونًا واحدًا خامًا؛ كله توكنز تتبع الحزمة الفعّالة |
| النصّ بتدرّج مقصوص (`background-clip: text`) للنسبة | **مرفوض** | المجموعة 02. والنسبة رقم يُقرأ، فيأخذ لون نصّ يقيس تباينه |
| ظلال داخلية خمس مكدّسة على بطاقة التبويب | **مرفوض** | زخرفة لا تحمل معلومة؛ حدّ واحد وظلّ واحد يؤدّيان الدور |
| `animation: folderFloat 3s infinite` — طفو دائم | **مرفوض — واستُبدل** | القاعدة في `bridge.css`: الحلقات اللانهائية للحركيات المحيطة والحالة وحدها. الطفو يقول «أنا حيّ» ولا يقول شيئًا عن الحالة |
| `overflow: hidden` على `body`، وعرض ثابت 550px | **مرفوض** | يكسر التمرير والاستجابة معًا |

## ما أضفناه لأن المكوّن لا يكون مكوّنًا بدونه

الأصل عرضٌ ساكن: عدّاد يصل إلى 100 ويقف. تحويله إلى قطعة في نظام تصميم يعني ما يلي:

- **حالة لكل ملف** — `queued | uploading | done | failed` بدل نسبة واحدة عامّة، فالملف الذي أخفق لا يختفي داخل متوسّط.
- **مخرج من كل حالة** — إلغاء أثناء النقل عبر `AbortController` حقيقي يوقف الرفع لا يخفيه فقط، وإعادة محاولة بعد الإخفاق.
- **الإلغاء ليس إخفاقًا** — قرار من المستخدم، فيُزال الصفّ بدل أن يُصبغ بلون الخطر.
- **واجهة رفع قابلة للتوصيل** — `upload?: Uploader` تستقبل الملف ودالة تقدّم وإشارة إلغاء. الافتراضي محاكاة **مُعايَرة بحجم الملف**، فالكبير يستغرق أطول فعلًا.
- **الغطاء يُحرّكه الحال لا مؤقّت** — مغلق عند الفراغ، مفتوح أثناء الهبوط والسحب فوقه، موارب بعد الاستقرار. زاوية واحدة تحمل ثلاث حالات.
- **الوصول** — `role="progressbar"` بقيمه، ومنطقة `aria-live` للحالة، وأزرار لها أسماء تذكر اسم الملف، وإدخال ملفات أصلي خلف زرّ.
- **الاتجاهان** — كل المسافات منطقية (`insetInlineStart`, `marginBlockStart`)، فالمجلّد ولسانه ينقلبان مع `dir` بلا قاعدة إضافية.
- **تفضيل تقليل الحركة** — يرثه المكوّن من `.madar-surface` في `bridge.css` دون شيفرة خاصة.

## أين يعيش

- المكوّن: `src/madar/components/upload.tsx` — `UploadFolder`, `simulatedUpload`
- العرض: `src/madar/showcase/sections/Upload.tsx` — قسم `madar-upload` في عائلة «التحكّم والمدخلات»
- البوابات: `gates/06-upload.md`

`FileDropzone` في `essentials.tsx` باقٍ كما هو ولم يُدمج معه: ذاك **حقل** يختار ملفات داخل نموذج، وهذا **بطاقة وجهة** ترفعها. وظيفتان مختلفتان، لا نسختان من واحدة.
