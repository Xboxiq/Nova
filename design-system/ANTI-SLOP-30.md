# معيار anti-slop-ui: الثلاثون قاعدة، ومكانها من هذا المشروع

المالك سلّم مهارة `anti-slop-ui` وطلب تطبيقها. المهارة مثبَّتة في
`.claude/skills/anti-slop-ui/SKILL.md`، وهذا الملفّ هو **مصفوفة التحقّق** التي
تطلبها هي نفسها في آخرها — لكن مملوءة بحالة حقيقية لا بمربّعات فارغة.

ثلاثة أشياء تلزم قبل الجدول:

1. **القاعدة تُقاس لا تُذكَر.** `tools/qa/anti-slop-30.mjs` يشغّل ما يمكن قياسه
   آليًّا، ويطبع `ANTI_SLOP_30=ok` أو أسماء القواعد الساقطة بمواضعها.
2. **لا ماسح ثانٍ للنمط نفسه.** سبعة بنود يفهرسها ماسح `kill-ai-slop` في هذا
   المستودع سلفًا، فهي **مفوَّضة** إليه وسقوفها في `gates/03` و`gates/04`. عدّاد
   ثانٍ للشيء نفسه رقمٌ ثانٍ يجب مزامنته.
3. **الإعفاء يُكتَب بجانب الشيفرة.** الماسح يحترم توجيه
   `anti-slop-ignore-next-line <rule>` بسببه، كما يفعل الماسح القائم. الإعفاء بلا
   سبب مكتوب ليس إعفاءً بل ثقب.

---

## الجدول

| # | القاعدة | الحالة | الدليل |
|---|---|---|---|
| 01 | لا تدرّجات قاسية | مفوَّض | `kill-ai-slop` 02 و06؛ الاستثناء الوحيد تخطيط بحدود حادّة يحمل بيانات (§11) بسببه مكتوب في `bridge.css` |
| 02 | لا خلفية بيضاء صافية | قائم | `--nova-canvas` هو `#f3f7f8` فاتحًا و`#0d1b22` داكنًا. **وضُيِّق الفحص:** نقطة نجمة بـ3px وطبقة تمرير بيضاء 7٪ ليست «لوحة بيضاء تُعمي البصر» |
| 03 | لا تلوين قوس قزح | قائم | لون تمييز واحد رمزي، ولوحة `CATEGORICAL` مُصدَّقة للتصنيف فقط، ولوحة `TIER` الأربعة ترميزٌ ثابت (§13) |
| 04 | لا بنفسجي على أسود | قائم | لا وجود لـ`#8b5cf6` ولا `#a855f7` في المستودع |
| 05 | لا ألوان نيون | **أُصلح** | الفحص نظيف، والإعفاء الوحيد شريط منتقي الصبغة: مساره هو دائرة اللون نفسها، وسببه مكتوب عند السطر |
| 06 | لا باستيل باهت | مفوَّض | «بلاطات الأيقونات الباستيلية» بند مفهرس في الماسح القائم، ومرفوض في الدفعة الأولى من التغذية البصرية |
| 07 | لا ظلال ساقطة | **تعارض مسجَّل** | انظر أدناه |
| 08 | لا زجاج سائل | **تعارض مسجَّل** | انظر أدناه |
| 09 | لا زوايا مفرطة الاستدارة | **تعارض مسجَّل** | انظر أدناه |
| 10 | لا شريط لوني على الحافة اليسرى | قائم | لا وجود لـ`border-left: Npx solid <color>` في المستودع |
| 11 | لا كرات ضوء شعاعية | قائم | كل `radial-gradient` في القشرة **لمعة على جسم صغير** (`.brand-mark`، `.stage-avatar`، `.material-orbit > span`) لا كرة عائمة في زاوية خلفية |
| 12 | لا شبكات نقاط أو خلفيات شبكية | **تعارض مسجَّل** | انظر أدناه |
| 13 | لا ثلاث بطاقات في صفّ | حكم يدوي | القشرة تستعمل `auto-fit` لا `grid-cols-3`؛ وفي التوثيق ثلاث خلايا حين يكون المعروض ثلاثة أشياء |
| 14 | لا شبكات بينتو | حكم يدوي | `BentoGrid` **مفردة مفهرسة** في المكتبة لا تخطيطًا تسويقيًّا للقشرة. انظر أدناه |
| 15 | لا نافذة طرفية مزيّفة | **أُصلح** | حُذفت النقاط الثلاث (كورال/عنبري/نعناعي) من موكاب البطل، وصار الشريط يسمّي المنتج المعروض. والقاعدة نفسها في §10 |
| 16 | لا علامات صحّ كنقاط تعداد | **أُصلح** | أُزيلت من `SurfaceDemos` و`PatternStudio` و`content` و`physics` و`social` ومن ختم الفاتورة. والصحّ الذي **يبلّغ حالة** (مربّع اختيار، صفّ منجَز) حالةٌ لا نقطة تعداد، وسببه بجانبه |
| 17 | لا ثلاث درجات أسعار | حكم يدوي | `SquishyPricing` مفردة مكتبة. القشرة لا تبيع شيئًا ولا تعرض أسعارًا |
| 18 | عرض منتج حقيقي | قائم | القشرة **هي** المكوّنات الحقيقية تعمل: ٣٢ قسمًا تفاعليًّا لا صور نائبة |
| 19 | لا زوايا حبّة الدواء | **تعارض مسجَّل** | انظر أدناه (نفس §9 من الجدول) |
| 20 | لا بنفسجي على أسود في الوضع الداكن | قائم | سبع حزم، وثلاثتها الداكنة على أزرق-رمادي عميق لا على أسود بنفسجي |
| 21 | إجبار الهياكل الهيكلية | **أُصلح** | `MadarStageSkeleton` بأبعاد المسرح الحقيقية: سطر تسمية، وعنوان، وسطران، وشبكة خلايا. كان السقوط الاحتياطي سطر نصّ يترك المسرح فارغًا ثم يقفز |
| 22 | لا كرات ضوء | قائم | مطابق للبند 11 |
| 23 | لا شبكات نقاط | **تعارض مسجَّل** | مطابق للبند 12 |
| 24 | لا أيقونات لمعان | **أُصلح** | **صفر** في المستودع. أُزيلت من ١٦ موضعًا، وعلامة الهوية صارت `BrandMark` مرسومة: أحرف NOVA لا لمعة سحر |
| 25 | لا أسهم متحرّكة | قائم | لا `animation: bounce` ولا سهم تمرير نابض. **وضُيِّق الفحص:** سهم `↓` في شارة فرق قياسي بيانات لا إيماءة |
| 26 | شروط استخدام | **أُصلح** | `LegalDialog` بوثيقة حقيقية مكتوبة من سلوك الموقع الفعلي، وزرّ في التذييل لا رابط `#` ميت |
| 27 | سياسة خصوصية | **أُصلح** | الوثيقة الثانية في الحوار نفسه: ثلاث تفضيلات في التخزين المحلّي، ولا كوكيز، ولا قياس زيارات، ولا طلب إلى أي نطاق خارجي، واستضافة GitHub Pages مذكورة صراحةً |
| 28 | لا رفع عند التمرير | **أُصلح** | حُذف `translateY` من خمسة مواضع في القشرة، وحُذفت زيادة الظلّ عند تمرير البطاقة. لون الحدّ هو التبليغ كلّه |
| 29 | لا شهادات مزيّفة | حكم يدوي | القشرة لا تعرض شهادات. `TestimonialSlider` مفردة مكتبة ببيانات تجريبية. انظر أدناه |
| 30 | لا Inter ولا Geist ولا Space Grotesk | **أُصلح** | كان `--nova-font-mono` هو `Geist Mono Variable`. صار `JetBrains Mono` ثم `IBM Plex Mono` — كلاهما على قائمة المهارة المسموحة. وواجهة النصّ كانت أصلًا `IBM Plex Sans Arabic` وهي مسموحة |

`ANTI_SLOP_30=ok` عند كتابة هذا الملفّ.

---

## التعارضات: أربع قواعد تناقض قانون المشروع نفسه

هذه ليست بنودًا أهملتُها، بل بنودًا **تصطدم بقانون كتبه المالك على خمس دفعات من
التغذية البصرية**. ولا أحسمها وحدي، فالقانون له لا لي. الحكم المؤقّت في كلٍّ منها:
**القانون يسبق، لأنه أخصّ وأكثر تحديدًا، ولأن المالك أنشأه بنفسه.**

### 07 — «لا ظلال ساقطة» مقابل §3 «ثلاثة ظلال ولكل واحد وظيفة»

المعيار يطلب حدًّا بـ1px بدل الظلّ. و§3 يوجب **ثلاثة** ظلال بثلاث وظائف: ظلّ
تلامُس يعطي الوزن، وظلّ مصبوب يعطي الارتفاع، وانحجاب محيطي عند المَفصِل. وحذفها
يحوّل كل جسم في المكتبة إلى ما يسمّيه القانون **ملصقًا** — وهو أحد أوضح تلال
الـslop عنده.

**والفارق ليس شكليًّا:** ما يرفضه المعيار في مثاله السيّئ ظلٌّ بانتشار 25px بلا
حدّ. وظلال هذه المكتبة `0 1px 2px -1px` و`0 14px 26px -14px` — ضيّقة، منخفضة
الشفافية، **ومقرونة بحدّ 1px دائمًا**. أي أن نيّة القاعدة محقّقة وحرفَها ليس.

**المطلوب منك:** إن أردت حرف القاعدة، فهو حذف §1 و§3 من القانون وإعادة بناء
العدّاد والفاتورة ومجلّد الرفع بلا حجم. قل الكلمة وأفعلها، لكني لا أفعلها بلا قولك.

### 08 — «لا زجاج» مقابل `data-glass` كميزة معمارية

في نوفا **ثلاثة مستويات مادة** عبر `data-glass`، مبنية بالتوكنات ومعروضة في
القسم. حذف الزجاج ليس تنظيف صفحة بل **إزالة ميزة من نظام التصميم**.

ما فعلتُه بلا انتظار: كل سطح جديد كتبتُه في هذه التمريدة **صلب لا زجاجي** —
حوار الشروط والخصوصية سطح مصمت بنصف قطر 6px. وما لم أفعله: لم أمسّ ميزة الزجاج
القائمة.

### 09 و19 — «نصف قطر 6px حدًّا أقصى» مقابل أجسام المكتبة

بطاقات المكتبة على `--r-lg` (22px)، وأختام الشرائح على `--r-full`. والمعيار يريد
`0` إلى `6px`. هذا **تغيير هوية كامل** لثلاثين قسمًا، لا تنظيف.

وما التزمتُه: كل سطح جديد في هذه التمريدة عند 6px أو أقلّ.

### 12 و23 — «لا شبكات نقاط» مقابل §11 و§15

المعيار يرفض النقاط والشبكات **كخلفيات زخرفية**، وهذا مقبول عندنا حرفيًّا. لكن
التخطيط في هذه المكتبة **بيانات**: المدى المعتاد، والبقيّة، والتوقُّع (§15-ب)،
والمشط الذي كل شرطة فيه وحدة حقيقية (§15-أ). ليس خلفية ولا مزاجًا، وله توجيه
`deslop-ignore` بسببه المكتوب.

**الحكم: لا تعارض فعليًّا** بعد قراءة القاعدة بنيّتها. سُجّل هنا لأن الماسح لا
يفرّق بين الاثنين بالنمط.

---

## مفردات المكتبة مقابل سطح القشرة

`BentoGrid` و`TestimonialSlider` و`SquishyPricing` و`NoiseDotCard` و`DotMatrixReadout`
مكوّنات **مفهرسة في مكتبة**، لا تخطيطات تسويقية تدّعي شيئًا. والمعيار يخاطب صفحة
هبوط تكذب على قارئها؛ ومكتبة تحفظ مفردةً لا تكذب على أحد.

وهذا هو المحور نفسه الذي أرساه المستودع سلفًا وينفّذه `tools/qa/slop-shell.mjs`:
**افتراضات المكوّنات التي تُشحَن إلى المنتجات تبقى نظيفة، ومفردات التوثيق معفاة.**
فالقشرة — `App.tsx` والتذييل والبطل والعروض — خُضِعت للثلاثين كاملة، وهي موضع كل
إصلاح في الجدول أعلاه.

**قرارٌ ينتظرك:** إن أردت حذف هذه المفردات الخمس من المكتبة أصلًا، فهو حذف
مكوّنات طلبتَها في مجموعة Madar المستوردة. أقول لك ما هي، ولا أحذفها بلا طلبك.

---

## حدّ في الوثيقتين القانونيّتين

الشروط وسياسة الخصوصية **مكتوبتان من سلوك الموقع الحقيقي**، ولا تدّعيان أكثر منه:
لا كوكيز، ولا قياس زيارات، ولا طلب إلى نطاق خارجي، وثلاثة مفاتيح في التخزين
المحلّي بأسمائها. وهما بلغة واضحة **لا مراجعة محامٍ**. وهذا كافٍ لصفحة ثابتة لا
تجمع شيئًا؛ ولا يكفي لحظة يظهر فيها حساب أو دفع أو نموذج — عندها تلزم وثيقتان
حقيقيتان.
