{
  "//": "NOVA's root package.json sets \"type\": \"module\", which would make Node parse these vendored CommonJS hooks as ESM and crash them. Nearest-package-json wins, so this pins the directory back to CommonJS. Do not remove.",
  "type": "commonjs"
}
